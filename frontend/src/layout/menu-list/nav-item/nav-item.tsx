import { NavItemType } from '@/types/menu';

type NavItemProps = {
  item: NavItemType;
  level: number;
  parentId: string;
};

export const NavItem = ({ item, level, parentId }: NavItemProps) => {
  // return null;

  return <></>;
};
