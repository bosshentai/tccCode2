import { AppBar } from '@/components/ui/extended/app-bar';
// import { Toast } from '@/components/ui/toast';
// import { useAuth } from '@/hooks/use-auth';

export const Landing = () => {
  // const { userNotFound, resetUserNotFound } = useAuth();

  return (
    <>
      <AppBar />
    </>
  );
};
